/* Values defined in cms */

export enum RoleEnum {
  Admin = 'Admin',
  User = 'User',
  Public = '',
}

export enum FolderEnum {
  Avatar = 'avatar',
}

export enum PathEnum {
  Login = '/account',
}
